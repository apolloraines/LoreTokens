### Integration Test (SAIQL-Delta)

`scripts/test_loretoken_integration.py` is an optional integration test that
assumes you have SAIQL-Delta installed or available via `SAIQL_HOME`.

If you don’t use SAIQL-Delta yet, you can ignore this script; the rest of the
LoreToken tools and examples work standalone.
